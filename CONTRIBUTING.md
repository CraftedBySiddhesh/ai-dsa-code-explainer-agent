# Contributing

## Local checks
```bash
pip install pre-commit
pre-commit install
pre-commit run -a
```

## PR rules
- Keep PRs focused
- Update README for user-facing changes
- Never commit `.env` / keys
